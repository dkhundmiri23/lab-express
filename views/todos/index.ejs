<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>express1</title>
</head>
<body>
    <h1>rick and morty</h1>
    <ul>
        <% characters.forEach(function(t) { %>
          <li>
            <%= t.characters %>
              - 
            <%= t.done ? 'done' : 'not done' %>
          </li>
        <% }); %>
      </ul>
</body>
</html>